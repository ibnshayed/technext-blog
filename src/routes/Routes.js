// const TableConfig = lazy(() => import("../components/pages/oushod sheba/tableConfig/TableConfig"));

const Routes = [
  // {
  // 		path: DASHBOARD_PATH,
  // 		exact: true,
  // 		isPrivate: true,
  // 		component: Dashboard,
  // 		permission: Permissions.ALL
  // },
];

export default Routes;
